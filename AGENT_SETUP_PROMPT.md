Install the package Skills, then run `$chatgpt-agent-setup`. ZIP mode is the
default and needs no Bridge setup. Configure MCP only when I explicitly choose
it; finish MCP setup after a real `workspace_info` call and
`agentctl.py doctor --json` reports `ready: true`.
