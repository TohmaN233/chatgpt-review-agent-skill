interface:
  display_name: "ChatGPT Review Agent"
  short_description: "Run ChatGPT packet reviews for code, papers, or drafts"
  default_prompt: "Use $chatgpt-review-agent. Pick the review prompt format that matches the artifact: code, academic paper, or writing draft. For Pro or any tool-less GPT reviewer, use packet review. For High/extra-high with a selected connector, smoke test a real MCP tool call first. Save the newest review markdown locally."
